apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: amqp-ingress
spec:
  entryPoints:
    - web
  routes:
  - match: HostSNI(`amqp.homelab.com`)
    priority: 10
    services:
    - name: one
      port: 5672
      terminationDelay: 400
      proxyProtocol:
        version: 1
