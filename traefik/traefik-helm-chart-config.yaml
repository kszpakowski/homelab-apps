apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    image:
      name: traefik
      # tag: v2.7.1
    # ports:
    #   amqp1: 
    #     port: 5672
    #     expose: true
    #     exposedPort: 5672