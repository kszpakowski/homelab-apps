apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ldap-crt
spec:
  secretName: ldap-tls
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - ldap-homelab-directory.svc.cluster.local
